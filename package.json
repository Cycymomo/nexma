{
  "name": "nexma",
  "version": "0.1.0",
  "author": "Cyril Moreau <cyril.moreau0804@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose up -d --build",
    "stop": "docker stop $(docker ps -a -q) && docker rm $(docker ps -a -q)",
    "clean": "docker rmi $(docker images -q)",
    "deploy-prisma": "cd ./server/prisma && npx prisma migrate save --experimental && npx prisma migrate up --experimental && npx prisma generate",
    "release": "now && now --prod"
  },
  "devDependencies": {
    "@prisma/cli": "^2.0.0"
  }
}
