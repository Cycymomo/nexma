# Migration `20200504151650-user`

This migration has been generated by Cyril at 5/4/2020, 3:16:50 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP TABLE `nexma`.`Post`;

DROP TABLE `nexma`.`User`;

CREATE TABLE `nexma`.`User` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`email` varchar(191) NOT NULL  ,`id` int NOT NULL  AUTO_INCREMENT,`name` varchar(191)  DEFAULT '' ,`password` varchar(191) NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `nexma`.`Post` (
`authorId` int  ,`content` varchar(191)   ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` varchar(191) NOT NULL  ,`published` boolean NOT NULL DEFAULT false ,`title` varchar(191) NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `nexma`.`User`(`email`)

ALTER TABLE `nexma`.`Post` ADD FOREIGN KEY (`authorId`) REFERENCES `nexma`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200415164705-init..20200504151650-user
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -12,16 +12,18 @@
   email     String    @unique
   password  String
   name      String?   @default("")
   posts     Post[]
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
 }
 model Post {
-  id        Int       @id @default(autoincrement())
+  id        String    @id @default(cuid())
   title     String
   content   String?
   published Boolean   @default(false)
+  author    User?     @relation(fields: [authorId], references: [id])
+  authorId  Int?
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
-  author    User?     @relation(fields: [authorId], references: [id])
-  authorId  Int?
 }
```


